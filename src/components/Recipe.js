import React from 'react';
import { data } from '../data';

const Recipe = ({data}) => {
    return (
        <>
            <div>
            
            </div>
        </>
    );
};

export default Recipe;